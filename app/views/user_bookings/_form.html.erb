<%= form_with(model: user_booking) do |form| %>
  <% if user_booking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_booking.errors.count, "error") %> prohibited this user_booking from being saved:</h2>

      <ul>
        <% user_booking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
